#include <stdio.h>

/**
 * main - program that prints alphabet in lowercase
 * Return : Always 0
 */
int main(void)
{
	char l;

	for (l = 'a'; l <= 'z'; l++)
	{
		putchar(l);
	}
	putchar('\n');
	return (0);
}
